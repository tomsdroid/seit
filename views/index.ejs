<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SEIT Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: #0b0f1a; }
        .glass { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(10px); }
        .custom-scrollbar::-webkit-scrollbar { width: 6px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #1e293b; border-radius: 10px; }
    </style>
</head>
<body class="text-slate-200 antialiased">

    <nav class="border-b border-slate-800 glass sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-6 py-4 flex justify-between items-center">
            <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-gradient-to-tr from-blue-600 to-indigo-600 rounded-xl flex items-center justify-center shadow-lg shadow-blue-500/20">
                    <span class="font-bold text-white">S</span>
                </div>
                <div>
                    <h1 class="text-lg font-bold tracking-tight text-white">SEIT <span class="text-blue-500">SYSTEM</span></h1>
                    <p class="text-[10px] text-slate-400 uppercase tracking-widest font-semibold">Automation</p>
                </div>
            </div>
            <div class="flex items-center gap-2 bg-green-500/10 px-3 py-1.5 rounded-full border border-green-500/20">
                <span class="relative flex h-2 w-2">
                    <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                    <span class="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
                </span>
                <span class="text-xs font-bold text-green-400 tracking-wide">ONLINE</span>
            </div>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto px-6 py-10">
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
            <div class="glass p-6 rounded-3xl border border-slate-800 shadow-sm transition hover:border-blue-500/50">
                <p class="text-slate-400 text-xs font-semibold uppercase mb-2 tracking-wider">Total Grup Partner</p>
                <div class="flex items-end gap-2">
                    <h3 class="text-5xl font-black text-white"><%= totalGroups %></h3>
                    <span class="text-blue-500 font-bold text-sm mb-1">Nodes</span>
                </div>
            </div>
            <div class="glass p-6 rounded-3xl border border-slate-800 shadow-sm">
                <p class="text-slate-400 text-xs font-semibold uppercase mb-2 tracking-wider">Jaringan Aktif</p>
                <div class="flex flex-wrap gap-2 mt-3">
                    <span class="bg-blue-600/20 text-blue-400 px-3 py-1 rounded-lg text-[10px] font-black border border-blue-500/20 tracking-tighter">TELEGRAM</span>
                    <span class="bg-indigo-600/20 text-indigo-400 px-3 py-1 rounded-lg text-[10px] font-black border border-indigo-500/20 tracking-tighter">DISCORD</span>
                    <span class="bg-pink-600/20 text-pink-400 px-3 py-1 rounded-lg text-[10px] font-black border border-pink-500/20 tracking-tighter">INSTAGRAM</span>
                </div>
            </div>
            <div class="glass p-6 rounded-3xl border border-slate-800 shadow-sm">
                <p class="text-slate-400 text-xs font-semibold uppercase mb-2 tracking-wider">Owner Identity</p>
                <h3 class="text-xl font-bold text-white"><%= ownerId %></h3>
                <p class="text-[11px] text-blue-400 font-mono mt-2 flex items-center gap-1">
                    <span class="w-1 h-1 bg-blue-400 rounded-full animate-pulse"></span>
                    Verified Administrator
                </p>
            </div>
        </div>

        <div class="glass rounded-3xl border border-slate-800 shadow-2xl overflow-hidden">
            <div class="px-8 py-6 border-b border-slate-800 flex justify-between items-center bg-slate-900/40">
                <div>
                    <h2 class="text-xl font-bold text-white">Database Jaringan</h2>
                    <p class="text-xs text-slate-400 mt-1">Informasi distribusi grup & topik forum</p>
                </div>
                <button onclick="location.reload()" class="bg-slate-800 hover:bg-slate-700 text-white px-5 py-2.5 rounded-xl text-xs font-bold transition-all active:scale-95 border border-slate-700">
                    REFRESH
                </button>
            </div>
            
            <div class="overflow-x-auto custom-scrollbar">
                <table class="w-full text-left">
                    <thead>
                        <tr class="bg-slate-900/50 text-slate-500 text-[10px] uppercase tracking-[0.2em]">
                            <th class="px-8 py-5 font-bold">Group & Topic Name</th>
                            <th class="px-8 py-5 font-bold">Group ID</th>
                            <th class="px-8 py-5 font-bold">Topic ID</th>
                            <th class="px-8 py-5 font-bold text-right">Status</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-800/50">
                        <% if (groups.length > 0) { %>
                            <% groups.forEach(function(group) { %>
                            <tr class="hover:bg-slate-800/20 transition group">
                                <td class="px-8 py-6">
                                    <div class="font-bold text-white text-sm group-hover:text-blue-400 transition">
                                        <%= group.title || group.name %>
                                    </div>
                                    <div class="text-[10px] text-slate-500 font-bold mt-0.5 flex items-center gap-2">
                                        <span class="text-blue-500/70">TOPIC:</span> 
                                        <span class="text-slate-400 tracking-wide uppercase"><%= group.topic_name || 'General' %></span>
                                    </div>
                                </td>
                                <td class="px-8 py-6">
                                    <span class="font-mono text-[11px] bg-slate-900 px-2 py-1 rounded text-slate-400 border border-slate-800">
                                        <%= group.id %>
                                    </span>
                                </td>
                                <td class="px-8 py-6">
                                    <% if (group.topic_id) { %>
                                        <span class="font-mono text-[11px] bg-indigo-500/10 text-indigo-400 px-2 py-1 rounded border border-indigo-500/20">
                                            #<%= group.topic_id %>
                                        </span>
                                    <% } else { %>
                                        <span class="text-slate-700">—</span>
                                    <% } %>
                                </td>
                                <td class="px-8 py-6">
                                    <div class="flex justify-end items-center gap-2">
                                        <div class="w-1.5 h-1.5 bg-green-500 rounded-full shadow-[0_0_8px_rgba(34,197,94,0.4)]"></div>
                                        <span class="text-[10px] font-black text-green-500 uppercase tracking-tighter">Linked</span>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="4" class="px-8 py-20 text-center">
                                    <p class="text-slate-500 text-sm font-medium italic">No active nodes detected in the database.</p>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <footer class="mt-10 text-center">
            <p class="text-slate-600 text-[10px] uppercase tracking-[0.4em] font-bold">SEIT Automation Engine • System v2.0</p>
        </footer>
    </main>

</body>
</html>
